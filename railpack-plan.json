{
  "build": {
    "commands": [
      "npm ci",
      "cd client && npm ci",
      "cd client && npm run build",
      "ls -la client/build || echo 'Build directory check failed'"
    ]
  },
  "start": "npm start"
}
