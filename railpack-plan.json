{
  "build": {
    "commands": [
      "npm ci",
      "cd src/client && npm ci",
      "cd src/client && npm run build",
      "ls -la client/build || echo 'Build directory check failed'"
    ]
  },
  "start": "npm start"
}
