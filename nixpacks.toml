[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = [
  "npm install --legacy-peer-deps --no-audit --no-fund",
  "cd client && npm install --legacy-peer-deps --no-audit --no-fund"
]

[phases.build]
cmds = ["cd client && npm run build"]

[start]
cmd = "node server.js"

[variables]
NODE_ENV = "production"
